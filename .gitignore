# The family's actual records: database and uploaded files.
data/

# Build output and the workspace-local Go toolchain.
dist/
school-nanny
school-nanny.exe
.toolchain/

# Local builds of the TOC converter.
tools/toc2yaml/*.exe
tools/toc2yaml/*.pdb

# Coolify API tokens and anything else Ansible needs but nobody else should see.
ansible/vault.yml
ansible/*.retry

