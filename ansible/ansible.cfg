[defaults]
inventory = inventory.ini
roles_path = roles
host_key_checking = False
# One host, short playbooks: the extra detail is worth more than the brevity.
stdout_callback = yaml
interpreter_python = auto_silent

[privilege_escalation]
become = True
become_method = sudo
